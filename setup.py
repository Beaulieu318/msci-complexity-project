from setuptools import setup, find_packages

setup(
    name='msci_complexity_project',
    version='0.1',
    long_description='Msci Complexity Project.',
    packages=find_packages(),
    install_requires=[],
)
